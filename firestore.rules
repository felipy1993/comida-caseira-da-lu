rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite ler e gravar apenas se o usuário estiver logado no Firebase Auth
      allow read, write: if request.auth != null;
    }
  }
}
